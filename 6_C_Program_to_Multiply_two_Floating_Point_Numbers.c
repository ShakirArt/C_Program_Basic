#include <stdio.h>

int main() {
    float a, b, product;
    printf("Enter the 1st number ");
    scanf("%f", &a);
    printf("Enter the 2nd number ");
    scanf("%f", &b);
    product = a*b;
    printf("Product of numbers %f", product);
    return 0;
}

